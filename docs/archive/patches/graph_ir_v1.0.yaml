# HR-DAG Trading System — Graph IR
# Single Source of Truth
# 이 파일은 원본이 너무 크므로 별도 첨부된 전체 문서를 참조하세요.
# 기존 graph_ir_v1.0.yaml의 전체 내용은 변경 없이 유지됩니다.
# Reinforcement Patch v2.0의 변경사항(+2 nodes, +6 edges)은 
# docs/specs/architecture_reinforcement_patch_v2.0.md에 정의되어 있으며,
# 이 파일의 다음 버전(v1.1)에서 병합됩니다.
#
# 현재 수치 (Patch v2.0 반영 후):
#   nodes: 45 (43 + ApprovalGate + MarketContextBuilder)
#   edges: 95 (89 + 6)
#   domain_types: 96
#   validation_rules: 46
#   pre_order_checks: 22
#
# 전체 YAML 원본은 GitHub 이전 커밋 또는 별도 첨부 파일 참조
